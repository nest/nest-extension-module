version: 2

sphinx:
  builder: html
  configuration: doc/conf.py
  fail_on_warning: true

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

# Specify Python dependencies for reproducible builds
python:
  install:
    - requirements: doc/requirements.txt
